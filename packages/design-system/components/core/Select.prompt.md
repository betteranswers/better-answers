One-line: a native `<select>` wearing the field shell — keyboard and screen-reader behaviour comes free ([A11Y1]).

```jsx
<Select label="Sensitivity" options={["Restricted","Internal","Public"]} defaultValue="Restricted" />
```

Six or more choices, or anything needing search, belongs in a command palette rather than a select.
