One-line: a setting that applies the moment it is flicked (optimistic, reconciled after — `[UX2]`); use a Checkbox when the change waits for a Save.

```jsx
<Switch label="Withhold home addresses" description="Default-on for this binding." checked={on} onChange={setOn} />
```
