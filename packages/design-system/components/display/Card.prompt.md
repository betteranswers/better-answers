One-line: the only container — hairline border, 10px radius, shadow so slight it reads as a line; never a coloured left border, never a gradient.

```jsx
<Card title="Sources" meta="14 bindings · 3 parked" actions={<Button size="sm">Add a source</Button>} footer="Map as of 09:41 · 30 August 2026">
  …
</Card>
```

`elevated` only for something that genuinely overlays. `interactive` darkens the border on hover — no lift, no scale.
