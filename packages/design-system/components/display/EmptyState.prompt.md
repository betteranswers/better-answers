One-line: the honest empty — say what is not there and offer the one act that fixes it; never an illustration, never an apology.

```jsx
<EmptyState title="Nothing on the map answers this."
  description="Three passages from connected documents mention it. Suggest a concept from one of them."
  action={<Button variant="accent" size="sm">Suggest a concept</Button>} />
```
