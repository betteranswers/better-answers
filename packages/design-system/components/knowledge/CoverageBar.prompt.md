One-line: how much of a section's expectation the map actually fills — always shown as a count first, the bar second.

```jsx
<CoverageBar included={9} expected={13} label="Security section" />
```

Amber while anything is missing, green when whole. The words "9 of 13 · 4 missing" carry it alone if colour is removed.
