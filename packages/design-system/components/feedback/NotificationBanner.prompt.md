One-line: a persistent state of the surface — needs review, map unavailable, extraction ceiling reached — with the act that clears it.

```jsx
<NotificationBanner tone="warning" heading="Needs review"
  action={<Button size="sm">Open the change</Button>}>
  A concept this section includes changed on 28 August 2026.
</NotificationBanner>
```

Always leads with the heading word the glossary uses; never a toast for the same content.
