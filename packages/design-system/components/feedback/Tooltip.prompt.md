One-line: a name or a keystroke for an icon-only control — never the second level of disclosure, which is `Details` ([UX1]).

```jsx
<Tooltip label="Copy with citations  ⌘C"><IconButton label="Copy"><Icon name="copy"/></IconButton></Tooltip>
```
