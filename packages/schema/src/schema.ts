/**
 * The `public` schema Drizzle owns (ADR 0007). Empty until B2 (`T-003`), which brings
 * the first tables, the workspace id on every row, the role model and
 * `FORCE ROW LEVEL SECURITY` — and, with the first `pgTable`, the `drizzle-orm`
 * dependency this package does not yet have a consumer for.
 */

export {};
