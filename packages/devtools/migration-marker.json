{
  "description": "The one spelling of the marker a hand-written migration opens with, said once for the gate and the strip. It names the decision that allows hand-written SQL, so it is data both readers match whole and never prose either of them writes: the Python check reads this file directly and the strip's syntax table through scripts/config-comments.mjs, which exports it to the proof's own fixture. Change the marker in the migrations and here in the same commit, or the gate deletes what the strip keeps.",
  "marker": "-- Custom migration (hand-written SQL; ADR 0032)."
}
