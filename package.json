{
  "name": "better-answers",
  "private": true,
  "description": "Better Answers — a living company knowledge map for UK SMBs on OKF v0.2 (see AGENTS.md)",
  "engines": { "node": ">=24 <25", "pnpm": ">=10" },
  "packageManager": "pnpm@11.24.0",
  "scripts": {
    "check": "pnpm -r --if-present run check && (cd worker && uv run --frozen check)",
    "migrate": "pnpm --filter @better-answers/schema run migrate",
    "mutation": "pnpm -r --if-present run mutation"
  }
}
