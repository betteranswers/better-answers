"""Better Answers — the Python knowledge worker (ADR 0005).

One image, one container in v0.1. It owns everything that turns sources into
indexed, governed knowledge; it shares a contract of data with the app tier and
never code, and it never migrates a schema (ADR 0007).
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
