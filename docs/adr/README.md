# The decision record, one line each

The **live conclusion** of every ADR — the state after its amendments, not its opening claim. Read this before trusting any single ADR's body: the convention is chronological (a change is a new ADR or an amendment, never a rewrite), so a body can be superseded by text further down its own file or in a later number; a sentence an amendment supersedes is struck (`~~…~~`) with a dated pointer to the amendment, and is never otherwise rewritten. This index exists because that convention has twice cost a session a stale conclusion.

**Maintenance rule: a PR that adds an ADR, or an amendment that changes a conclusion, updates the matching line here in the same commit.**

| ADR | Live conclusion |
| --- | --- |
| 0001 | ~~Type vocabulary authoritative in the bundle~~ — **superseded by 0026**: no vocabulary file; kinds are read off the concepts (superseded 2026-08-29) |
| 0002 | Concept identity is a platform-minted opaque IRI on the bare apex `better-answers.com`; bundle identity lives in the manifest at the bundle's root, `knowledge/manifest.yaml`; `okf://` is an MCP wire URI and never a file reference (2026-08-30) |
| 0003 | One tenant bundle of atom concepts in v0.1; domains are future bundle boundaries; merge-by-identity is keyed `(workspace, bundle, type, normalised label)` in `concept_identity` (2026-08-25) |
| 0004 | Guides and compositions are platform records citing concepts, never restating them; a Q&A pair is an `Answer` concept and a context wording lives on its concept (0011, 0014); a guide's readers are levels Admin · Editor · Viewer; no publish state; the map degrades visibly in fixed words (2026-08-28) |
| 0005 | Own app, two runtime tiers (TS app, Python worker) sharing **four stores and never code**; no app↔worker HTTP — the control plane is rows; third-party parts lifted as pinned snapshots under `THIRD_PARTY_NOTICES.md`; the tier contract's fixtures live in top-level `contracts/` (0031) (2026-09-01); the LIFT family of rules is retired from the constitution — lifting by contract stands as this ADR's, the per-lift file is `[APP4]`'s, the licence posture ADR 0027's own text (T-078, 2026-09-05) |
| 0006 | Hono long-running Node server + Vite SPA, not Next.js; the api serves the SPA's static build on `app.`; `apps/web` imports `apps/api` as a type only, for `AppRouter` (2026-09-02) |
| 0007 | Plain Postgres; the app owns every migration and all DDL — in `index` and the graph too; deploy order `migrate` → `api` → `worker`; the graph engine and custom image are superseded by 0032 (2026-09-01) |
| 0008 | tRPC inside; generated OpenAPI, MCP and `/agent/v1` outside; the identity provider's pages are `/oauth2/*`, not `/oauth/*`; the MCP surface and `/oauth2/*` are on the app origin, not `mcp.` (0034), and the host router holds paths per hostname across three hostnames — `apps/api/src/ingress/hostnames.ts` (2026-09-03); the one-test-per-capability-through-every-transport clause is retired from `[SEC2]`, a transport proven by its own tests and a capability through `[TEST1]`'s interface (T-078, 2026-09-05) |
| 0009 | Better Auth in-process — the app is its own authorization server; its tables are the identity set, isolated by key not by scope, and its organisation model is the `workspace` table (2026-09-01); sign-in and the picker are the SPA's, CIMD allow-listed to `claude.ai`, the `admin()` plugin refused as shipped (2026-09-02); **one origin, one session** — the cookie is host-only `__Secure-` and consent is safe on the product's origin because of the closed client list plus PKCE, not because of the other host (0034, 2026-09-03); the identity seam is kept for `packages/core`'s independence and for the callers that never hold a session, **not** for a provider swap — none is planned — a token's workspace is fixed at consent time, and T-028's operator is a third principal kind rather than a threaded `admin()` role (2026-09-04); **a person has one id** — the `user` row's, minted by the platform's minter that Better Auth is handed too — the `admin()` refusal protects it, and revocation has two scopes: a workspace Admin by an instant on the membership row, the operator everywhere (0035, 2026-09-05); the identity seam is this ADR's own decision, its rule tag retired, both fences lint-enforced with a rule test each (T-078, 2026-09-05) |
| 0010 | ~~Typed relations on concepts~~ — **rejected**; a link is the relation (0026) |
| 0011 | Three knowledge layers (sources → bundles → graph) and the minting rule decide where a unit lives; the graph derives from sources too, as source entities that are never concepts; a relation is a `LINKS_TO` edge, never a typed derivation (0026) (2026-08-30) |
| 0012 | The bundle is written only by the app, one commit per act; platform-prepared changes wait as suggestions; enrichment jobs read committed concepts, a run may enrich its own candidates before submitting (2026-09-01); the forge consequence is superseded — bare repositories (0024); the erasure routine mailmaps author lines to the erasure pseudonym, never a member id (0035, 2026-09-05); **the `audit_event` id is minted before the commit and rides in the `Audit:` trailer**, the slice owns the act's transaction and holds the per-repository lock from the precondition through the Postgres COMMIT so `bundle_commit` is a prefix of git history, and the reconciler's action is defined — replay the missed commits oldest-first through the live handler under `process:better-answers-reconciler`, idempotent on the trailer id; a revocation landing inside the act's window does not stop the replay (authorization is judged at time-of-act, against the instant the acting credential was issued at, under a shared lock on the member and person rows — so **rows** after the instant are impossible by construction while a bare commit in the window is the replay case) and unwanted content is undone by a forward revert, never a history rewrite (T-052, 2026-09-06); the replay's ledger row is the reconciler's under the commit's `Audit:` id, what a commit does not carry — merge key, class, evidence — is recovered fail-closed, an acceptance decided in the window lands as the commit it is, a live write on an unrecorded head fails on the parent key and the next tick heals it, and a commit the index refuses stops the replay there — reported, never skipped, the operator's to put right (T-056, 2026-09-07); a replayed file whose `sources[]` are not the standing citations lands Restricted and its ledger row says so, because a lost commit that changed what a concept cites never replays at the class the citations it lost derived (2026-09-08) |
| 0013 | Sources bound by origin, reach and destination; publish · sensitivity · audience are the three permission fields, applied as one server-side predicate on every read (2026-08-30); its lift tags are retired — the per-lift file is `[APP4]`'s and the licence list 0027's (T-078, 2026-09-05); the audience is two columns, `audience` ∈ *everyone · groups* beside `audience_groups text[]` under one CHECK (0039, 2026-09-08) |
| 0014 | Records attach by IRI, versioned where edited, audited in one ledger; findings, subject requests and entity merges are record families in that ledger (2026-08-29); the member row carries no id of ours — a person is `human:<person id>` on every record, the user row is what erasure pseudonymises, and the ledger is never rewritten (0035, 2026-09-05); the ledger is an ordinary tenant table, **not partitioned** — a row-count trigger reopens it — insert-only by revoked privilege, its id caller-minted and its family and subject kind derived from the act, and `team` is no record of the identity set (0038, 2026-09-06); the ledger gains a unique `(workspace_id, id)` so a record may key to the row it was written with, `concept_owner` is a table on the `concepts` slice with a per-domain default and never a column, a composition's two homes are declared together, and the by-IRI key and the version column set are one helper each (T-113, 2026-09-10) |
| 0015 | A composition cites its concept by a footnote labelled by the include (roles are levels, 2026-08-30) |
| 0016 | An answer asserts concepts only, found by traversal, served as one contract — verdict first; at depth 0 when the map is unavailable, said in the header line (2026-08-30); **in v0.1 the entry step is full-text over the concept index alone** — a stored `tsvector` written in the governed write's transaction, searchable at commit, no model call — and the concept unit of the one index with its vectors and catch-up run is the reserve block S8, landing only when the answer tests' recall on the first client's context wordings falls below S2's threshold (2026-09-09) |
| 0017 | Every answer is a retained, correctable record; an `Answer` is minted only at a gate a person runs (2026-08-30) |
| 0018 | One MCP surface at `app.<domain>/mcp` (0034, 2026-09-03), the Principal from the token grown by scope — **four entries, not the title's five** (A26, ticket 79; `describe_estate` dropped, 2026-08-31) |
| 0019 | Trust is derived from the file, told in fixed words, moved only by a check; verifier ≠ generator; `human:<email>` is rewritten only on erasure, to `human:<erasure pseudonym>` — a per-workspace id minted at erasure, never the person id (0035, 2026-09-05) |
| 0020 | Personal data is withheld at the seam before any store; the erasure routine rewrites every actor id the platform wrote, and a person edits what the company wrote — the title's *every copy* is narrowed to that (2026-08-30); the rewrite target is a per-workspace erasure pseudonym kept on the request, the user row is pseudonymised with its id kept, the ledger is never rewritten, and the erasure map gains the identity set's invitation emails, verification identifiers, session IP and user agent and linked account id (0035, 2026-09-05); **no v0.1 block embeds** — the embedding route row stays fixed and unread until the reserve block S8, and Mistral enters the DPIA input and the sub-processor list only from the day a workspace's embedding route is first called (0016, 2026-09-09) |
| 0021 | ~~Graph on Neo4j Community~~ — **superseded by 0023** |
| 0022 | Two stacks deployed by digest; every irreplaceable byte encrypted off-host; erasures replayed on restore; `app.` is open at the edge — Better Auth is its sign-in (2026-09-02); the tunnel's ingress rules are the first fence and the app's own hostname list the second; **three hostnames, not four** — `mcp.` is gone and `docs.` is struck, `APP_HOSTNAME` is derived from `PUBLIC_URL`, the tunnel has three ingress rules and the rate-limit rules match paths on `app.` (0034, 2026-09-03); the backup identity is resident on VPC 2 as well as escrowed, the backup image is the third digest, every promotion is a `RELEASES.md` row and Q7's switch is a check in `release.yml`, `restore-production.sh` is the production restore and the `pnpm ops` commands answer done · refused · not built (T-005, 2026-09-03); the ops documents moved to `docs/operations/` (T-119, 2026-09-10) |
| 0023 | ~~Apache AGE, one graph per workspace~~ — **engine superseded by 0032** (plain tables under RLS); its write model stands: the read predicate lives once, in the app's graph query module; the incremental delta joins the app's commit transaction (2026-08-29 amendment); the read predicate's columns — `published_at`, `sensitivity` and the audience pair `audience` · `audience_groups` on `concept_index`, `composition`, every `index.chunk` row and the graph tables — are recorded here, out of `[SEC2]` (T-078, 2026-09-05); ~~a unit with no evidence takes the workspace default~~ — a unit resting on nothing takes its fallback, audiences combine by intersection with *everyone* the identity, and an empty intersection forces Restricted (0039, 2026-09-08); ~~the per-query timeout on the graph role~~ — set per statement in the graph door's `walk` at S2's budget, and entry by key admits a set — the seed is `ANY(uids)` with one shared cap, the reached set and the cut depth recorded on the answer audit (T-113, 2026-09-10) |
| 0024 | The forge is bare git repositories the app writes; the estate is two 4 GB boxes; a model-host box is a precondition of local embedding, triggered by the first workspace that asks (2026-08-30); `GIT_STORE_DIR` names the forge's root, one repository per workspace directly under it, checked once by the git door at open (T-105, 2026-09-08) |
| 0025 | A signal is a query over rows the platform already keeps; every model call writes one `llm_call` row; the `llm_call` columns, the answer audit's own table and the alert-once rule are recorded here, out of `[LOG1]` and `[OPS1]` (T-078, 2026-09-05) |
| 0026 | Kinds emerge from the concepts — no vocabulary file; a link is the relation, never named; the five named edges are the platform's own bookkeeping (2026-08-30) |
| 0027 | Open core under Apache-2.0; the hosted service is the product; copyleft is run-only; the estate's own configuration is not published — the ops documents' public halves name classes, contracts and shape, the private file holds addresses and edge-policy placement (2026-08-30); the constitution's LIFT family is retired and the posture binds as this ADR's own text, the per-lift file `[APP4]`'s, the root aggregate and the `check` licence step struck (T-078, 2026-09-05) |
| 0028 | A boundary schema is generated from its table; a refinement only narrows; a parity test proves it; a `customType`'s plain schema is constructed and tested per shape (2026-09-01) |
| 0029 | The tree is `apps/` over `packages/`; business logic is `packages/core` — capability slices over four store doors; import direction is a lint rule (its contracts-rename and six-item-risk consequences are enacted and corrected by 0031); the tenancy rule's demotion is enacted — the tag is retired, and the store door is `packages/core/src/store/`, exported as `@better-answers/core/store` (T-078, 2026-09-05); the tree gains `workspaces/` and `members/` as built (T-048, 2026-09-06); the graph door is the one store module that imports `access` — a traversal template must be unable to exist without the predicate — and the tree's graph line sheds its AGE/Cypher wording (T-053, 2026-09-07); rule 4's `index.ts` clause is a `no-restricted-imports` pattern with a both-ways test, not a convention (T-114, 2026-09-10); all five import-direction rules are one plugin rule, `better-answers/import-direction`, placing both ends of an import in a zone by position under the package whose manifest names `@better-answers/core`, with the `exports` map as the face list and the `packages/core/**` override deleted — the `layers.json` sentence is struck, and inside a slice the slice is the unit (T-117, 2026-09-10) |
| 0030 | The MCP surface stays MCP SDK v2 in the TypeScript tier behind one fetch-shaped seam (four entries, 2026-08-31) |
| 0031 | The tier contract is six agreements in three forms (SQL function / fixtured / generated) in top-level `contracts/`, conformance-tested by both tiers' suites; the read predicate's logic is app-only — the cross-tier piece is the visibility columns the worker writes; ~~six agreements~~ **seven** — **id-shape**, fixtured, pins the one shape every id either tier mints, and `contract_version` bumps to 1 (0035, T-074, 2026-09-05); ~~seven~~ **eight** — **concept-file**, fixtured, pins the canonical text and content hash a concept file has whichever tier read it, and `contract_version` bumps to 5 (PR #51, 2026-09-08); ~~eight~~ **nine** — **redaction**, fixtured, pins the category list with each category's tier, the placeholder word per tier and category (one neutral `[withheld]` for the always set), the special-category → *Restricted* narrowing and the version string's shape `rule_version:detector_pin`, deriving nothing from a detector, and `contract_version` bumps to 6 (0020, T-120, 2026-09-10) |
| 0032 | The graph is plain Postgres tables under RLS (no AGE, no per-workspace roles, no custom image); one migration journal, one `current_workspace_id()` policy seam, one SECURITY DEFINER lifecycle function, `vector(N)` fixed, the worker's schema view generated and drift-checked both ways; the constitution's tenancy rule — workspace on every row, RLS default-deny with the identity set the one exemption, the graph tables as tenant tables, the `neo4j` ban — is retired to this ADR, its checkable sentences to `[SEC2]` and `[SEC3]` (T-078, 2026-09-05) |
| 0033 | The interface is Tailwind v4 with the design system's tokens as its theme through one bridge; shadcn, Kibo UI and AI Elements own behaviour, the platform owns meaning; the design system's own components are deleted; the components landed in `apps/web/src/shared/ui/` on 03/09/2026 with four how-it-is-written rules relaxed over that directory alone, and Kibo's `table` rejected as v8-bound; ~~better-auth-ui taken as its provider, its localization and its headless hooks (03/09/2026)~~ — **reversed 05/09/2026 (T-046)**: the auth module's own TanStack Query factories over the better-auth client replace all three, the package and its two `declare module` augmentations leave the tree, its 28 rendered sign-in files stay refused as product this platform does not have, and `bundler` stands on the app's own grounds |
| 0034 | One origin, `app.<apex>`, carries the SPA, sign-in, the picker, consent, `/oauth2/*`, discovery, `/jwks` and `/mcp`; three hostnames with `APP_HOSTNAME` derived from `PUBLIC_URL`; the session cookie is host-only `__Secure-` and `__Host-` is a written trigger; consent is server-rendered on that origin, safe there because of the closed client list plus PKCE and fenced to a document navigation; sign-in is an email code or Microsoft, never a password, with the per-client `sso` shape a written trigger (2026-09-03); the Pro trigger's premise corrected — plans meter rules not paths (Free 1 · Pro 2 · Business 5), the four path groups combined to fit, and the wizard passes on Free until the first client credential (2026-09-04); the ops documents moved to `docs/operations/` (T-119, 2026-09-10) |
| 0035 | A person has one id — the identity set's `user` row's, minted by the platform's one ULID minter that Better Auth is handed too, `userId` on every Principal and `human:<person id>` on every record the platform keeps, never in a concept file; the member row's key names nothing of ours; revocation has two scopes — a workspace Admin by an instant on the membership row, the operator everywhere — with a fresh sign-in minting anew on both; erasure rewrites `human:<email>` to a per-workspace erasure pseudonym kept on the request and never rewrites the ledger; the audit slice's two doors and the ledger's four families (people · knowledge · sources · platform, `family.subject.verb`) are the interface T-059 codes (2026-09-05) |
| 0036 | The worker composes cocoindex's building blocks — per-component commit, memoisation, stable ids, target sync, `mount_each`, timeouts, handlers, stats, one `Environment` per binding — and writes only what the engine has no block for — the run key, claim/lease/heartbeat/reaper, attempts and poison, the catalogue, retention, priced-vs-actual, outcome rows, the landing, one run per binding, supervision; `use_state` never (public but undocumented), `use_mount` never on the critical path; the exit stays cheap and every target is `managed_by="user"` (2026-09-05); a wipe is paired with deleting the binding's chunk rows because the LMDB is the target-state tracking, the memo holds only redacted text — conversion and the detector inside one memoised function — and `pipeline/` is the one module that imports `cocoindex`, lint-refused elsewhere (T-113, 2026-09-10) |
| 0037 | A reader-facing screen has a latency budget — lists under a second, actions under 100 ms optimistically and reconciled after, answers streamed — asserted by the browser suite per screen; a missed budget is a bug; `[UX2]` keeps the keyboard clause (2026-09-05) |
| 0038 | The people data model — groups flat and Entra-aligned as the one grouping shape (`audience_groups` holds their ULIDs, the implicit kind is the origin discriminator's, nothing mints one yet, a deleted group's dangling ids fail closed); an access request is a signed-in non-member's ask, made at the auth boundary under the platform principal with the person as actor, answered with one neutral acknowledgement, approved by a direct invitation write through the identity-write seam; the ledger founded as an ordinary tenant table the database keeps append-only — workspace-less acts stay log lines until T-028's identity-set ledger, provisioning is a platform act in the workspace it creates, the two doors reject inside the caller's transaction, month partitioning superseded by a row-count trigger; teams, plural roles, dynamic roles and temporal access not adopted (2026-09-06) |
| 0039 | An audience is `audience` ∈ *everyone · groups* beside `audience_groups text[]` of ADR 0038's group ids, tied by one CHECK on every readable unit and the binding, tested by a fail-closed `&&` conjoined with the other two terms (the Admin arm is the class's alone); audiences combine by intersection with *everyone* the identity, beside ADR 0023's most-restrictive class, derived at write time and re-derived synchronously two levels down on a narrowing, the map's copies rewritten in the same transaction; an empty intersection forces the unit Restricted and is never stored; a Restricted binding's named people are a `group` row of the implicit origin, minted by nothing here (2026-09-07) |
| 0040 | A `Clock` — `{ now(): Date }` — is a kernel value, not a door; the api constructs one `systemClock()` per process and hands it (or the `Date` it reads) explicitly to the six sites that used to read the wall clock ambiently — `open`'s trust reading, a concept's first `published_at`, an invitation's expiry, the git door's commit instant, and the Postgres door's two rate-limit counters — with no defaulted parameter anywhere; a row's own timestamp stays the database's `now()` and the worker is handed no Clock, its one clock-shaped read being the ULID minter's; the gate is a repo-tree scan (`apps/api/tests/no-ambient-clock.test.ts`), oxlint 1.80.0 shipping no `no-restricted-syntax` to express it as a config override (2026-09-08); the identity set's four `$onUpdate(() => new Date())` hooks were dead code — Better Auth stamps those tables' `updated_at` from inside the library (ADR 0009) — and are gone, the gate's scan gained `packages/schema/src` as a third root with the ULID minter as its second exemption, and the two raw-SQL writes to the identity set (`revokeCredentials`, the session's active-workspace fallback) now set `updated_at = now()` (T-109, 2026-09-09) |
