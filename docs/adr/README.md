# The decision record, one line each

The **live conclusion** of every ADR — the state after its amendments, not its opening claim. Read this before trusting any single ADR's body: the convention is chronological (a change is a new ADR or an amendment, never a rewrite), so a body can be superseded by text further down its own file or in a later number; a sentence an amendment supersedes is struck (`~~…~~`) with a dated pointer to the amendment, and is never otherwise rewritten. This index exists because that convention has twice cost a session a stale conclusion.

**A row is what is true now**, in a sentence or two, carrying one date — the newest amendment it accounts for, else the decision's own — which `apps/api/tests/adr-index.test.ts` holds it to. The amendment history, the struck-through text and the ticket ids stay in the ADR, the one place they are read in order: a row that instead grows a clause per amendment is a line any two tickets amending that ADR conflict on. A row in that shape is rewritten to this one when its ADR is next amended, not in a sweep.

**Maintenance rule: a PR that adds an ADR, or an amendment that changes a conclusion, updates the matching line here in the same commit.**

| ADR | Live conclusion |
| --- | --- |
| 0001 | ~~Type vocabulary authoritative in the bundle~~ — **superseded by 0026**: no vocabulary file; kinds are read off the concepts (superseded 2026-08-29) |
| 0002 | Concept identity is a platform-minted opaque IRI on the bare apex `better-answers.com`; bundle identity lives in the manifest at the bundle's root, `knowledge/manifest.yaml`; `okf://` is an MCP wire URI and never a file reference (2026-08-30) |
| 0003 | One tenant bundle of atom concepts in v0.1; domains are future bundle boundaries; merge-by-identity is keyed `(workspace, bundle, type, normalised label)` in `concept_identity` (2026-08-25) |
| 0004 | Guides and compositions are platform records citing concepts, never restating them; a Q&A pair is an `Answer` concept and a context wording lives on its concept (0011, 0014); a guide's readers are levels Admin · Editor · Viewer; no publish state; the map degrades visibly in fixed words (2026-08-28) |
| 0005 | Own app, two runtime tiers (TS app, Python worker) sharing **four stores and never code**; no app↔worker HTTP — the control plane is rows; third-party parts lifted as pinned snapshots under `THIRD_PARTY_NOTICES.md`; the tier contract's fixtures live in top-level `contracts/` (0031) (2026-09-01); the LIFT family of rules is retired from the constitution — lifting by contract stands as this ADR's, the per-lift file is the api's lift-contract rule's, the licence posture ADR 0027's own text (T-078, 2026-09-05); docling is a dependency of nothing — cocoindex and Google's OKF reference agent are what make Python unavoidable (T-130, 2026-09-20); the envelope's format is written down at last — **AES-256-GCM** in a versioned frame of *version · nonce · ciphertext · tag*, the version its first byte and also its authenticated data, a fresh nonce per seal, a version a reader does not know refused rather than guessed, an opener in each tier answering `envelope-version-unknown` · `envelope-malformed` · `envelope-not-authentic`, the golden vector at `contracts/credential-envelope/`, and ADR 0041's credentials provider still the first slice's that needs one (T-274, 2026-09-22) |
| 0006 | Hono long-running Node server + Vite SPA, not Next.js; the api serves the SPA's static build on `app.`; `apps/web` imports `apps/api` as a type only, for `AppRouter` (2026-09-02); "talk only over tRPC" means everything with a shape crosses compiler-checked through that type — the Better Auth client and bytes sit beside it, bytes in as the tRPC octet-stream mutation, bytes out as a route beside tRPC the landing block's ADR opens (2026-09-22) |
| 0007 | Plain Postgres; the app owns every migration and all DDL — in `index` and the graph too; deploy order `migrate` → `api` → `worker`; the graph engine and custom image are superseded by 0032 (2026-09-01) |
| 0008 | tRPC inside; generated OpenAPI, MCP and `/agent/v1` outside; the identity provider's pages are `/oauth2/*`, not `/oauth/*`; the MCP surface and `/oauth2/*` are on the app origin, not `mcp.` (0034), and the host router holds paths per hostname across three hostnames — `apps/api/src/ingress/hostnames.ts` (2026-09-03); the one-test-per-capability-through-every-transport clause is retired from the Principal-first rule, a transport proven by its own tests and a capability through the interface a caller crosses (T-078, 2026-09-05) |
| 0009 | Better Auth in-process — the app is its own authorization server; its tables are the identity set, isolated by key not by scope, and its organisation model is the `workspace` table (2026-09-01); sign-in and the picker are the SPA's, CIMD allow-listed to `claude.ai`, the `admin()` plugin refused as shipped (2026-09-02); **one origin, one session** — the cookie is host-only `__Secure-` and consent is safe on the product's origin because of the closed client list plus PKCE, not because of the other host (0034, 2026-09-03); the identity seam is kept for `packages/core`'s independence and for the callers that never hold a session, **not** for a provider swap — none is planned — a token's workspace is fixed at consent time, and T-028's operator is a third principal kind rather than a threaded `admin()` role (2026-09-04); **a person has one id** — the `user` row's, minted by the platform's minter that Better Auth is handed too — the `admin()` refusal protects it, and revocation has two scopes: a workspace Admin by an instant on the membership row, the operator everywhere (0035, 2026-09-05); the identity seam is this ADR's own decision, its rule tag retired, both fences lint-enforced with a rule test each (T-078, 2026-09-05); provisioning and the first membership land ahead of the console as two ops commands, and the identity-set ledger is deferred by date — the operator's identity behind an ops command and every workspace-less act stays a log line until T-028 (2026-09-22) |
| 0010 | ~~Typed relations on concepts~~ — **rejected**; a link is the relation (0026) |
| 0011 | Three knowledge layers (sources → bundles → graph) and the minting rule decide where a unit lives; the graph derives from sources too, as source entities that are never concepts; a relation is a `LINKS_TO` edge, never a typed derivation (0026) (2026-08-30) |
| 0012 | The bundle (a bare repository, 0024) is written only by the api, through governed acts: one commit per act under the per-repository lock held through the Postgres COMMIT, the `audit_event` id minted first and carried in the `Audit:` trailer, the act's rows in one transaction the slice owns — so `bundle_commit` is a prefix of git history — and the manifest is such a commit too, landing no concept row; platform-prepared changes wait as suggestions, and the erasure routine mailmaps author lines to the erasure pseudonym, never a member id (0035). The reconciler replays the missed commits oldest-first under `process:better-answers-reconciler`, idempotent on the trailer id, its ledger row under the commit's `Audit:` id: a concept commit through the live handler, what the commit does not carry recovered fail-closed and a file whose `sources[]` disagree with the standing citations landing Restricted; a manifest commit as its commit row alone; and a commit the index refuses stops the replay there, reported, never skipped — a live write on an unrecorded head fails on the parent key and the next tick heals it. A revocation inside the act's window does not stop the replay (authorization is judged at time-of-act) and unwanted content is undone by a forward revert, never a history rewrite; moving a ref back to its watermark is the operator's plumbing on a runbook page — not that rewrite, since it drops only commits no row recorded, and not an ops command — rehearsed on staging against a made-up stuck bundle before the first client's data is on the box (2026-09-22) |
| 0013 | Sources bound by origin, reach and destination; publish · sensitivity · audience are the three permission fields, applied as one server-side predicate on every read (2026-08-30); its lift tags are retired — the per-lift file is the api's lift-contract rule's and the licence list 0027's (T-078, 2026-09-05); the audience is two columns, `audience` ∈ *everyone · groups* beside `audience_groups text[]` under one CHECK (0039, 2026-09-08); a document may carry a class of its own that only narrows its binding's and the derivation reads the narrower, a publish is refused until the binding is indexed, and the destination is stored per connector and fed to the chunk index alone until S7 (T-128, 2026-09-11); the publish act reads the latest `index` job by subject and only *done* lets it through, the state word being written by the app's acts — *landed* by the bind, *published* by the publish — and rendered from the job between them (T-131, 2026-09-11); **a media type has one converter, chosen once**, because its normalised text is the address space every span and `content_hash` is read against — markdown and plain text pass through, `.docx` is `anydoc`'s and PDF `pdf-inspector`'s alone, each converter's name and version inside the memoised function's version, so an upgrade is a chosen reprocess; no model and nothing leaves the worker, *pure Python* having been a proxy for *no model*; a page with no text layer quarantines its document whole; docling is off the route — it wants more than twice the worker's cap — and OCR or a layout model starts at 0024's precondition (T-130, 2026-09-20); a binding's credential class is ADR 0041's list and no longer the credential-class rule's, both citations struck and repointed, the binding's own decision unchanged (T-184, 2026-09-21) |
| 0014 | Records attach by IRI, versioned where edited, audited in one ledger; findings, subject requests and entity merges are record families in that ledger (2026-08-29); the member row carries no id of ours — a person is `human:<person id>` on every record, the user row is what erasure pseudonymises, and the ledger is never rewritten (0035, 2026-09-05); the ledger is an ordinary tenant table, **not partitioned** — a row-count trigger reopens it — insert-only by revoked privilege, its id caller-minted and its family and subject kind derived from the act, and `team` is no record of the identity set (0038, 2026-09-06); the ledger gains a unique `(workspace_id, id)` so a record may key to the row it was written with, `concept_owner` is a table on the `concepts` slice with a per-domain default and never a column, a composition's two homes are declared together, and the by-IRI key and the version column set are one helper each (T-113, 2026-09-10) |
| 0015 | A composition cites its concept by a footnote labelled by the include (roles are levels, 2026-08-30) |
| 0016 | An answer asserts concepts only, found by traversal, served as one contract — verdict first; at depth 0 when the map is unavailable, said in the header line (2026-08-30); **in v0.1 the entry step is full-text over the concept index alone** — a stored `tsvector` written in the governed write's transaction, searchable at commit, no model call — and the concept unit of the one index with its vectors and catch-up run is the reserve block S8, landing only when the answer tests' recall on the first client's context wordings falls below S2's threshold (2026-09-09) |
| 0017 | Every answer is a retained, correctable record; an `Answer` is minted only at a gate a person runs (2026-08-30) |
| 0018 | One MCP surface at `app.<domain>/mcp` (0034, 2026-09-03), the Principal from the token grown by scope — **four entries, not the title's five** (A26, ticket 79; `describe_estate` dropped, 2026-08-31); **`find`'s hit is a union by knowledge layer**, not the body's "preview of hits by kind, bundle, type and tags" — a concept hit (`layer: "bundles"`, its fields as before) beside a document hit (`layer: "sources"`, kind *document*, title, wire locator, sensitivity word, rendered with the marker *Not company knowledge*), a document a visible concept cites omitted and the caller's limit spent on the union with the concepts first; **the two arms rank separately until S2**; and `open` takes an IRI or a wire locator — one, never the body's "one or many" — answering the passage or *not found*, with `open` by IRI rendering each evidence item's locator (T-134, 2026-09-21) |
| 0019 | Trust is derived from the file, told in fixed words, moved only by a check; verifier ≠ generator; `human:<email>` is rewritten only on erasure, to `human:<erasure pseudonym>` — a per-workspace id minted at erasure, never the person id (0035, 2026-09-05) |
| 0020 | Personal data is withheld at the seam before any store; the erasure routine rewrites every actor id the platform wrote, and a person edits what the company wrote — the title's *every copy* is narrowed to that (2026-08-30); the rewrite target is a per-workspace erasure pseudonym kept on the request, the user row is pseudonymised with its id kept, the ledger is never rewritten, and the erasure map gains the identity set's invitation emails, verification identifiers, session IP and user agent and linked account id (0035, 2026-09-05); **no v0.1 block embeds** — the embedding route row stays fixed and unread until the reserve block S8, and Mistral enters the DPIA input and the sub-processor list only from the day a workspace's embedding route is first called (0016, 2026-09-09); **one declared descriptor per redaction category** is the record the analyzer's registry, the entity table, the agreement's category list and `rule_version` all derive from, so a rule change is one record and one bump; `content_hash` stays the hash of the original normalised text computed **before** the seam runs, with the version string carried beside the redacted text, which closes the spike's open question; and the detector's versions are re-read on the day of the change — gliner at 0.2.29, the body's 0.2.28 struck (T-121, 2026-09-11); **the routine as built** — the report's beyond-use dates run from the instant it took `pg_advisory_lock(41)`, are named as the anchor used until O1's `backup_run` moves it, and are calendar intervals computed as Postgres computes `backup_run.expires_at`, never counts of days; the identity set is pseudonymised on the person's last membership and this workspace's membership ends on both arms, the judgement the platform principal's and never an Admin's, with the invitations on that arm deleted in **every** workspace — the one write of the step that leaves the erasing one, because an invitation is keyed by the address and not by the person (S0 review, 2026-09-11); the mirror's prune after a `--mirror` push is the backup service's (T-125 lands the line); the replay copy under the platform prefix carries the identifier set and the pseudonym the report is starved of, and is written before the completion commits; the api image carries git and `git-filter-repo` 2.47.0 from PyPI with its interpreter, the rejected alternative a dulwich rewrite in the worker; and `subject_request` names its subject by an identifier set beside a nullable person id with `clock_started_at` and `extended_to`, an access answer being locations and categories and never a passage (T-124, 2026-09-11); **a finding is the same finding on every run that finds it** — the document, the rule and the two offsets, unique on the row, the worker inserting `ON CONFLICT DO NOTHING` — so `reprocessBinding` deletes only the findings no Admin has reviewed or restored, R9's delete is narrowed to those and its premise withdrawn — *keep in text* queues its own run and never calls `reprocessBinding` — and a restore reaches the worker as an argument of the memoised function beside the suppressions, read through a column-level SELECT on the identity columns and `restored_at` alone, the seam leaving a restored span in the text unless an unrestored finding overlaps it **or an erasure request names it — an erasure outranks a restore**, a suppression raising the tier of the very finding that was restored, and the run naming the kept spans an erasure overrode on its outcome row so the review can say beside a finding group that a keep did nothing; what becomes of a document's findings when its `content_hash` moves is left open, S4's to decide (T-132, 2026-09-20); ~~a rule change is one record and one bump~~ **a rule with no descriptor behind it bumps `rule_version` too** — the seam also holds rules about what the model is shown, and the first is where a **window** into a page begins (the word settled in `CONTEXT.md` against the index's *chunk*): **a window begins where the document itself begins something** — a run under a heading read whole to twice Presidio's window, that run cut at its paragraphs when it is longer, and only a paragraph longer still stepped through by a count on whole words — so GLiNER is never handed a fragment of a word and what it is shown of one piece no longer depends on how much text sits above it, with the count returning only for a document that is one unbroken paragraph past the ceiling, a floor and not an exception; the size and overlap stay the detector's and so `detector_pin`'s, the ceiling is derived from the size and swept rather than chosen, and the descriptor digest is unchanged beside the moved version; the whole-word, sentence, line, heading-only and blank-line-everywhere rules were each built and measured and are recorded as rejected — this one is the only one that levels the planted page at T-168's four lengths **and** a heading-less page at eight, leaving the planted page's twenty-four spans exactly as `main` raises them and taking two wrong ones off the heading-less page — a `person-name` over *driver*, already a `job-title`, and one over `'ives broken'`, a mid-word fragment — while gaining none; and the seam's milliseconds are re-read on the image against the page as it now is, 4447/4345/5022 ms over 3,107 bytes, with T-130's constants left where they are and the arithmetic recorded for the owner (T-177, 2026-09-21); **a run refreshes its own reading of a span it finds again** — category, tier, score and the version pair, through column-level UPDATE and SELECT on those five and nothing an Admin wrote, a restored row keeping the tier it was restored at — so the review reads the last run, and **a finding whose version pair is not its document's `redaction_version` is one the last run did not raise**, left out of the review read, both bulk acts and the publish totals (T-132, 2026-09-21) |
| 0021 | ~~Graph on Neo4j Community~~ — **superseded by 0023** |
| 0022 | Two stacks deployed by digest; every irreplaceable byte encrypted off-host; erasures replayed on restore; `app.` is open at the edge — Better Auth is its sign-in (2026-09-02); the tunnel's ingress rules are the first fence and the app's own hostname list the second; **three hostnames, not four** — `mcp.` is gone and `docs.` is struck, `APP_HOSTNAME` is derived from `PUBLIC_URL`, the tunnel has three ingress rules and the rate-limit rules match paths on `app.` (0034, 2026-09-03); the backup identity is resident on VPC 2 as well as escrowed, the backup image is the third digest, every promotion is a `RELEASES.md` row and Q7's switch is a check in `release.yml`, `restore-production.sh` is the production restore and the `pnpm ops` commands answer done · refused · not built (T-005, 2026-09-03); the ops documents moved to `docs/operations/` (T-119, 2026-09-10); **the replay runs after all three stores are back**, still before `api` starts, on the `api` service rather than `migrate` because only that one carries the git store and the object store's settings — and Garage's root key and bucket, which no script had ever created, are provisioned by `wizard-41.sh` with `S3_BUCKET` and `S3_REGION` on the platform compose anchor (T-125, 2026-09-11); forward-only migrations and the digest rollback are this ADR's decision alone and never the deploy rule's — nothing checks either half and neither is legible in a diff, which is what separates them from the deploy rule's sentences that stay, held to varying degrees and each now saying how far (T-184, 2026-09-21) |
| 0023 | ~~Apache AGE, one graph per workspace~~ — **engine superseded by 0032** (plain tables under RLS); its write model stands. The read predicate lives once, in the app's graph query module, and is tested against columns — `published_at`, `sensitivity`, `audience` · `audience_groups` — on `concept_index`, `composition` and the graph tables, and against `index.readable_chunk`'s for a chunk, whose visibility is read from its binding and document and not carried (0044); a class is derived most-restrictive and re-derived synchronously two levels down on a narrowing, a unit resting on nothing takes its fallback, audiences combine by intersection and an empty one forces Restricted (0039); the walk's timeout is per statement and entry by key admits a set; the governed write refuses a citation of an uncatalogued document (2026-09-21) |
| 0024 | The forge is bare git repositories the app writes; the estate is two 4 GB boxes; a model-host box is a precondition of local embedding, triggered by the first workspace that asks (2026-08-30); `GIT_STORE_DIR` names the forge's root, one repository per workspace directly under it, checked once by the git door at open (T-105, 2026-09-08); ~~two verbs~~ **three** — the mirror key's forced command gains `prune-repo <ws>`, argument-checked as `init-repo` is, which reflog-expires and gc-prunes one mirror after a `--mirror` push whose `--porcelain` report says refs were replaced: ADR 0020's "gc on both copies" on VPC 2's side, run by the backup service because the push is the only thing that knows a rewrite happened (T-125, 2026-09-11) |
| 0025 | A signal is a query over rows the platform already keeps; every model call writes one `llm_call` row; the `llm_call` columns, the answer audit's own table and the alert-once rule are recorded here, out of the structured-logger rule and the deploy rule (T-078, 2026-09-05) |
| 0026 | Kinds emerge from the concepts — no vocabulary file; a link is the relation, never named; the five named edges are the platform's own bookkeeping (2026-08-30) |
| 0027 | Open core under Apache-2.0; the hosted service is the product; copyleft is run-only; the estate's own configuration is not published — the ops documents' public halves name classes, contracts and shape, the private file holds addresses and edge-policy placement (2026-08-30); the constitution's LIFT family is retired and the posture binds as this ADR's own text, the per-lift file the api's lift-contract rule's, the root aggregate and the `check` licence step struck (T-078, 2026-09-05) |
| 0028 | A boundary schema is generated from its table; a refinement only narrows; a parity test proves it; a `customType`'s plain schema is constructed and tested per shape (2026-09-01) |
| 0029 | The tree is `apps/` over `packages/`; business logic is `packages/core` — capability slices over four store doors; import direction is a lint rule (its contracts-rename and six-item-risk consequences are enacted and corrected by 0031); the tenancy rule's demotion is enacted — the tag is retired, and the store door is `packages/core/src/store/` (T-078, 2026-09-05); the tree gains `workspaces/` and `members/` as built (T-048, 2026-09-06); the graph door is the one store module that imports `access` — a traversal template must be unable to exist without the predicate — and the tree's graph line sheds its AGE/Cypher wording (T-053, 2026-09-07); rule 4's `index.ts` clause is a `no-restricted-imports` pattern with a both-ways test, not a convention (T-114, 2026-09-10); all five import-direction rules are one plugin rule, `better-answers/import-direction`, placing both ends of an import in a zone by position under the package whose manifest names `@better-answers/core`, with the `exports` map as the face list and the `packages/core/**` override deleted — the `layers.json` sentence is struck, and inside a slice the slice is the unit (T-117, 2026-09-10); ~~the store door is exported as `@better-answers/core/store`~~ — the empty `store` face is deleted and `./store` leaves the `exports` map, each door exported under its own name, `@better-answers/core/store/postgres` beside its three siblings, nothing having imported the barrel and rule 2 refusing to let it re-export a door (T-118, 2026-09-19); every one of the four doors is real in a test and none may be faked — *Real Postgres, always* becomes *real stores, always*, an in-memory adapter being for a service someone else runs, behind that service's own adapter (T-253, 2026-09-22) |
| 0030 | The MCP surface stays MCP SDK v2 in the TypeScript tier behind one fetch-shaped seam (four entries, 2026-08-31); the **acting** credential class stays defined here — for acts on our own estate and the ingestion side, never for writing into a customer's other systems — though `docs/operations/SECRETS.md` glosses it incompatibly and ADR 0041 records that conflict unsettled; the class *list* it belongs to is ADR 0041's, the credential-class rule's citation struck and repointed (T-184, 2026-09-21) |
| 0031 | The tier contract is twelve agreements in three forms — SQL function, fixtured, generated — in top-level `contracts/`, conformance-tested by both tiers' suites, a manifest entry's form held to the disk; the read predicate's logic is app-only. Its version is a digest of `contracts/` each tier computes at build, stamped by `migrate` and compared by the worker beside the schema stamp — a mismatch refuses the claim; the chunk leaves `visibility-columns` with ADR 0044's build, and what remains of that agreement is S6's to settle (2026-09-21); `citation` joins the set — what a citation looks like, compiled by each tier's comment gate and written out by neither (T-316, 2026-09-22); `credential-envelope` gets its fixture and the format half a sealed vector is worth nothing without, at `contract_version` 11, leaving `cost-ledger`'s golden rows the one declared form still owed the disk (T-274, 2026-09-22) |
| 0032 | The graph is plain Postgres tables under RLS (no AGE, no per-workspace roles, no custom image, and no `neo4j` ecosystem — this ADR's own ban); one migration journal, one `current_workspace_id()` policy seam, `vector(N)` fixed, the worker's schema view generated and drift-checked both ways; the tenancy rule lives here, its checkable sentences in the Principal-first rule and the tenant-table refusal rule. `worker_rt` is deny-by-default — a migration that wants the worker to reach a table says so in a GRANT, its SELECT on the `index.chunk` parent among them; `app_rt` keeps its defaults — and the roles' surface is read back, never written twice: a snapshot generated from `pg_catalog`, to columns and function EXECUTE, committed and drift-checked, beside one declared note naming the four SECURITY DEFINER functions and the tables each reaches (2026-09-21) |
| 0033 | The interface is Tailwind v4 with the design system's tokens as its theme through one bridge; shadcn, Kibo UI and AI Elements own behaviour, the platform owns meaning; the design system's own components are deleted; the components landed in `apps/web/src/shared/ui/` on 03/09/2026 with four how-it-is-written rules relaxed over that directory alone, and Kibo's `table` rejected as v8-bound; ~~better-auth-ui taken as its provider, its localization and its headless hooks (03/09/2026)~~ — **reversed 05/09/2026 (T-046)**: the auth module's own TanStack Query factories over the better-auth client replace all three, the package and its two `declare module` augmentations leave the tree, its 28 rendered sign-in files stay refused as product this platform does not have, and `bundler` stands on the app's own grounds |
| 0034 | One origin, `app.<apex>`, carries the SPA, sign-in, the picker, consent, `/oauth2/*`, discovery, `/jwks` and `/mcp`; three hostnames with `APP_HOSTNAME` derived from `PUBLIC_URL`; the session cookie is host-only `__Secure-` and `__Host-` is a written trigger; consent is server-rendered on that origin, safe there because of the closed client list plus PKCE and fenced to a document navigation; sign-in is an email code or Microsoft, never a password, with the per-client `sso` shape a written trigger (2026-09-03); the Pro trigger's premise corrected — plans meter rules not paths (Free 1 · Pro 2 · Business 5), the four path groups combined to fit, and the wizard passes on Free until the first client credential (2026-09-04); the ops documents moved to `docs/operations/` (T-119, 2026-09-10) |
| 0035 | A person has one id — the identity set's `user` row's, minted by the platform's one ULID minter that Better Auth is handed too, `userId` on every Principal and `human:<person id>` on every record the platform keeps, never in a concept file; the member row's key names nothing of ours; revocation has two scopes — a workspace Admin by an instant on the membership row, the operator everywhere — with a fresh sign-in minting anew on both; erasure rewrites `human:<email>` to a per-workspace erasure pseudonym kept on the request and never rewrites the ledger; the audit slice's two doors and the ledger's four families (people · knowledge · sources · platform, `family.subject.verb`) are the interface T-059 codes (2026-09-05) |
| 0036 | The worker composes cocoindex's building blocks — per-component commit, memoisation, stable ids, target sync, `mount_each`, timeouts, handlers, stats, one `Environment` per binding — and writes only what the engine has no block for — the run key, claim/lease/heartbeat/reaper, attempts and poison, the catalogue, retention, priced-vs-actual, outcome rows, the landing, one run per binding, supervision; `use_state` never, `use_mount` never fanning documents onto the critical path, `pipeline/` the one module that imports `cocoindex`, every target `managed_by="user"`. One memoised function, the detector's, returning spans: **no memo holds text**, conversion and the withholding run on every run, and the findings memo has a per-binding store a wipe spares. **Any deletion of a binding's chunk rows is paired with that store's removal** — a *rule-change* reprocess as much as a wipe, the rows deleted in the app's transaction and the store removed by the worker as the first statement of the run that deletion enqueued — because the store is the target-state tracking and a run over a standing store re-upserts nothing it believes it has landed. The memoised function's frozen identity is six — module, qualified name, version, mount path, app name and the store's directory — `version=1` being what takes its body out of the set; the spike measured all of it on cocoindex 1.0.22, with conversion plus the withholding at under a fifth of one percent of a detector pass over the same page (2026-09-22) |
| 0037 | A reader-facing screen has a latency budget — lists under a second, actions under 100 ms optimistically and reconciled after, answers streamed — asserted by the browser suite per screen; a missed budget is a bug; the web tier's UX rule keeps the keyboard clause (2026-09-05) |
| 0038 | The people data model — groups flat and Entra-aligned as the one grouping shape (`audience_groups` holds their ULIDs, the implicit kind is the origin discriminator's, nothing mints one yet, a deleted group's dangling ids fail closed); an access request is a signed-in non-member's ask, made at the auth boundary under the platform principal with the person as actor, answered with one neutral acknowledgement, approved by a direct invitation write through the identity-write seam; the ledger founded as an ordinary tenant table the database keeps append-only — workspace-less acts stay log lines until T-028's identity-set ledger, provisioning is a platform act in the workspace it creates, the two doors reject inside the caller's transaction, month partitioning superseded by a row-count trigger; teams, plural roles, dynamic roles and temporal access not adopted (2026-09-06) |
| 0039 | An audience is `audience` ∈ *everyone · groups* beside `audience_groups text[]` of ADR 0038's group ids, tied by one CHECK on every readable unit and the binding, tested by a fail-closed `&&` conjoined with the other two terms (the Admin arm is the class's alone); audiences combine by intersection with *everyone* the identity, beside ADR 0023's most-restrictive class, derived at write time and re-derived synchronously two levels down on a narrowing, the map's copies rewritten in the same transaction; an empty intersection forces the unit Restricted and is never stored; a Restricted binding's named people are a `group` row of the implicit origin, minted by nothing here (2026-09-07) |
| 0040 | A `Clock` — `{ now(): Date }` — is a kernel value, not a door; the api constructs one `systemClock()` per process and hands it (or the `Date` it reads) explicitly to the six sites that used to read the wall clock ambiently — `open`'s trust reading, a concept's first `published_at`, an invitation's expiry, the git door's commit instant, and the Postgres door's two rate-limit counters — with no defaulted parameter anywhere; a row's own timestamp stays the database's `now()` and the worker is handed no Clock, its one clock-shaped read being the ULID minter's; the gate is a repo-tree scan (`apps/api/tests/no-ambient-clock.test.ts`), oxlint 1.80.0 shipping no `no-restricted-syntax` to express it as a config override (2026-09-08); the identity set's four `$onUpdate(() => new Date())` hooks were dead code — Better Auth stamps those tables' `updated_at` from inside the library (ADR 0009) — and are gone, the gate's scan gained `packages/schema/src` as a third root with the ULID minter as its second exemption, and the two raw-SQL writes to the identity set (`revokeCredentials`, the session's active-workspace fallback) now set `updated_at = now()` (T-109, 2026-09-09); `account.updated_at` gets the `DEFAULT now()` its declaration has carried since T-004 and its DDL never did — one generated `ALTER`, no trigger, inert because Better Auth stamps the column on every write it makes, and the shape `user` and `verification` already had (T-138, 2026-09-12) |
| 0041 | A secret belongs to one of seven credential classes — bootstrap · ingestion · acting · agent · LLM provider · repository · object store — the bootstrap class read once from the environment by each tier's typed config module and the other six rows under the envelope, reached through a credentials provider no task has built (hashed behind a lookup prefix, expiring and revocable, one rotation path per class, every access audited); what each class is for and where it is read is written in `docs/operations/SECRETS.md`'s table, which record owns that gloss having never been settled, and *why* seven rather than one is recorded nowhere and is not invented here; **two conflicts are recorded unsettled** — what *acting* means (SECRETS.md against ADR 0030) and whether all six below bootstrap really are rows under the envelope (SECRETS.md's *where it is read* against the credential-class rule, which the object store, repository and agent rows contradict); the decision predates this record and was moved out of the credential-class rule, which keeps the seam and the never-mixed sentence (T-184, 2026-09-21) |
| 0042 | The product ships an accessibility statement; its contents, when it ships, where it is published, who writes it and the conformance claim it makes are all open, as they were when the sentence lived in the constitution; the reason — the buyers are UK public bodies for whom this is law, and the first client states it of its own products — justified the whole of the accessibility rule and **stays there**, quoted here rather than taken; the decision predates this record and was moved out of the accessibility rule, which keeps the axe gate, the acceptance line and the component semantics (T-184, 2026-09-21) |
| 0043 | An *act* is what an entry — a tRPC procedure, an MCP entry, an ops command, the tick — may ask core to do, a read or a write answering a `Result`; a *step* runs only inside another act's transaction and, after the act's first write, rejects; the constructor is **dropped** — the spike's probe 4 found GitNexus answering a lower bound for a constructed act — and the live shape is a declaration beside the function, its types derived, and a lint rule that a face function admits before its first `await` (T-237, 2026-09-22); a refusal word means one thing, is declared once per slice and registered globally, and carries one of seven classes by remedy (*unauthenticated · forbidden · absent · malformed · inapplicable · conflict · precondition*), append-only; one crossing function per transport carries `{ word, class }` (tRPC as a thrown error typed through `AppRouter`, ops as exit codes), procedures hand-written so the call graph stays whole; a transport never nests a transaction — query, mutation (membership read `FOR SHARE`) and own-transaction base procedures, the door rolling back on a refusal; input parsed once at the process entry by a kernel `parse` that owns the field map; instants as ISO-8601 text; the spike ran — the door rolls back with one suite case changing, the crossing types through `AppRouter`, zero suite lines move, and **the upload is a tRPC mutation over `application/octet-stream`**, `octetInputParser` on the own-transaction road with the descriptor beside the bytes, `FormData` withdrawn since tRPC buffers multipart, no ADR 0006 exception (2026-09-22) |
| 0044 | A chunk's visibility is read, not carried: `index.readable_chunk` (`security_invoker`) joins the chunk to its binding and its document, the class the narrower of the two by `narrower_class`, the ranking's one SQL statement; the four columns leave `index.chunk`, no tier writes a chunk's visibility, a narrowing or a publish is a write to the source row and queues no run, and a run commits its catalogue writes before it lands its chunks; a concept's and a composition's class stay columns, being derivations; reopened only by S8 measuring a need for the filter columns on the indexed table (2026-09-21) |
| 0045 | A coding rule is one imperative under a tag, in the `CODING_RULES.md` of the directory it binds — the root file, one per workspace, one beside the deployment configuration — with an optional `Reviewer:` line, a BAD/GOOD snippet where it helps, 80 words of prose a rule under a shrink-only exception list and 2,500 in the root file, naming identifiers only outside a snippet; a `Reviewer:` line is written only where no mechanism could catch a breach on the way in, a gap a gate could close being a ticket and no sentence, and a rule stating what it asks for rather than which gate catches a breach, the gate printing the tag itself; a tag is written in a rules file, a review finding or a gate's failure message and nowhere else; held by `apps/api/tests/coding-rules-form.test.ts` and `coding-rules-tags.test.ts`, each proving its parser on fixture text, and by the comment gates inside code (2026-09-22) |
