# T-125 — `replay-erasures` and `erasure-rehearsal`, the recovery order, the mirror's prune

**Ticket**: T-125 (`ordna show T-125`) · **Block**: s0, its **last** ticket — S0's PR opens when it lands
**Worktree**: `/Users/liamj/Documents/development/better-answers/.claude/worktrees/agent-ab3c08b87a8f32870`
**Branch**: `worktree-agent-ab3c08b87a8f32870` · **Head**: **`b70250a`** · tree clean · **6 ahead, 0 behind `s0`**
**Rebased onto `s0` in iteration 6**, cleanly, no conflict: the six commits replay from `e37037b` and every sha below moved (`d407784` → `b70250a`). The pre-rebase shas are kept in *Iteration history* so the one-commit ADR proofs stay traceable.
**This file is the record.** It lives at the main checkout's path only; the stale copy inside the worktree is abandoned, per *The task note*.

**Spec**: `docs/specs/s0-redaction-seam-and-erasure.md` — *The two ops commands* (145–149), *The routine* (125–143), *The acts on the ledger* (114–123), Testing Decisions seams 1 and 4.
**ADRs**: 0022 and 0024 both **amended by this ticket**; 0020 (lock 41, the mirror's prune, the replay copy — amended by T-124); 0029 (the api is transports); 0014 rule 4.
**Rules**: `docs/agents/build-loop.md`.

---

## Plan

Five waves, all landed. The ticket is code-complete and green; what remains is not build work but the orchestrator's own steps (below).

### Acceptance checklist — the ticket's lines, with the evidence for each

- [x] **`replay-erasures`** — *done* with *replayed 0 erasures* on the empty set with the table present; one request from the rows; one from the replay copy alone when the rows predate it; refusals for an absent git root and an unreachable object store; **the no-table answer unchanged**, still proved by `opsBeforeTheJournal` against the cluster's own `postgres` database. `tests/ops.test.ts` (49 cases) and `test/erasure-replay.test.ts` (9).
- [x] **`erasure-rehearsal`** — `--seed` prints the tokens on its last line; `--run --report` writes **the routine's own report**; the phases are separable by a dump because phase two re-finds the subject from rows by the derived address and takes only the workspace id. `test/erasure-rehearsal.test.ts` (8) and the ops suite.
- [x] **Both commands' ledger events under the platform principal** — `platform.erasure.replayed` (`REPLAY_ACTS`, `replay-erasures.ts`) and `platform.erasure.rehearsed` (`rehearsal.ts`), rows read back in the suites.
- [x] **The deploy-tree test holds the replay's position** after the object-store and git-store steps and before `api` in both restore scripts, **and drill step 10's seven-step order**. `tests/deploy-tree.test.ts`.
- [x] **The backup script prunes the mirror** after a `--mirror` push that replaced refs — keyed on `--porcelain`'s replaced-ref report, through `mirror-shell.sh`'s third verb — held by the deploy-tree test.
- [x] **ADR 0022's amendment and its README row in the same commit as the scripts**; **ADR 0024's amendment and its README row in the same commit as `mirror-shell.sh`**. Both proved by `git log -n1 --format=%H -- docs/adr/00NN-*.md` then `git show --name-only --format="" <sha>` before the rebase. **The api's `check`, the other half of this line, is green in iteration 6** — see *Test feedback*.

### Rule tags this ticket touches

| Tag | The sentence it binds here |
| --- | --- |
| `[TEST1]`–`[TEST9]` | Real Postgres and a real Garage in every suite; our own code never mocked; the routine's real `git filter-repo` run in the replay cases; expected values as literals. |
| `[GLOSSARY1]` | *erasure rehearsal*, *synthetic subject* and *token* landed in `CONTEXT.md` in wave 2, before code named them; ADR 0022's and 0024's amendments and their `docs/adr/README.md` rows landed in the same commits as the code they record. |
| The tag rule (`build-loop.md`, *The gates*) | A rule tag never appears in source, a deploy file, a Dockerfile, a CI workflow or a workspace config. **This ticket lost iteration 2 to it**; the four citations were rewritten as words with the tags cited in `erasure-replay.test.ts`. |
| ADR 0029 | The api is transports: the rehearsal's seed lives in core, not in the api and not in a `deploy/` script. |

### What landed, and what a caller reaches

| Wave | What | Commit (pre-rebase) | Evaluation |
| --- | --- | --- | --- |
| 1 | core — the replay's reading half | `0fc87f5` | PASS |
| 2 | core — the rehearsal's two phases | `d9205de` | FAIL on the tag scan → fixed in iteration 3 |
| 3 | api — the two commands, the config, the suite | `7be9492` | PASS |
| 4a | the recovery order, the stores, the wizard, ADR 0022 | `cfedd55` | PASS |
| 4b | the drill's proof, `dump-grep` per table, the mirror's prune, ADR 0024 | `6bdb938` | PASS (three readings) |
| 4b | step 7 refusing an empty hash set | `d407784` | in the same runs |

**Wave 1 — `packages/core/src/erasure/`, out of `@better-answers/core/erasure`:** `replay.ts` gains the reading half beside its writer — a boundary parse (`COPY_AS_READ`; unknown keys dropped, so a copy written by a later version is still an erasure that is owed; the person id optional and never null) and `replayCopiesSince`. **A store that will not answer is an error, never an empty set** — *none* is the one answer a restore must not hear. `replay-erasures.ts`: `erasureRequestsSince`, `replayableErasures` (rows ∪ copies, de-duplicated by erasure request id, ordered by completion then id), `restoreFromReplayCopy` (both rows re-created with **the copy's own pseudonym**, `completed_at` null so the routine's `ON CONFLICT` insert reads it back and one subject never ends with two names), `replayErasures` (each through `runErasure`, oldest first, **stopping at the first failure**, the error naming which request and how many had been replayed), `REPLAY_ACTS`. `PERSONAL_DATA_CATEGORIES` and `beyondUseFrom` **widened from module-private to exported**, with reasons.

**Wave 2 — `packages/core/src/erasure/rehearsal.ts`:** `seedSyntheticSubject` (a `user` row through the identity seam, an Admin membership in the workspace's scope, one concept file naming the subject in `verified[].by` through the governed write; answers with the three tokens `dump-grep --tokens` is handed) and `rehearseErasure` (records the request as that subject's own Principal, runs the routine, hands back **the routine's own report word for word** — a document built in the shape of a report would rehearse a formatter, and what an operator must read is what a data subject would). `packages/schema/src/table-ownership.ts`'s `erasure` reasons for `public.user` and `public.member` gained a clause: the seed writes both tables the way the routine only deleted.

**Wave 3 — `apps/api/`:** `readObjectStore` in `src/config.ts` (five settings, region defaulting to `garage`, the endpoint held to http or https; **optional**, since `migrate`'s environment has the database alone); `src/ops.ts` opening the door and calling `closeObjects` beside `pool.end()`; `OpsIo` gaining `objects` and `writeReport`, injected for the same reason `stdin` already was. `replay-erasures`' exit table: usage **2**; no table **0** (unchanged); empty set **0**; N replayed **0**; git root absent · no store configured · store unreachable · a routine failing all **1**, each ending *do not start api*. One expectation corrected with its docstring: **a row re-created from a copy is completed by the run that happens**, because `completed_at` is tied to a report by a CHECK. `packages/core/package.json` gained `"./testing/objects"` so the api suite starts a Garage without `apps/api` taking a `testcontainers` dependency.

**Wave 4a — the estate:** the replay after the object store and the git store and still before `api` (production step 6, drill step 5), run on the **`api` service** with `--no-deps` because only `api` carries `GIT_STORE_DIR` with `/data/git` mounted and the anchor's S3 settings; `S3_REGION: garage` and a **required** `S3_BUCKET` on the `x-bootstrap` anchor; **the Garage bootstrap that had never existed** — `garage key create` was only a comment at `stores.compose.yaml:74` — as an 11th wizard stage (the secret routed to the platform resource and escrow), with the drill's bucket create and `STAGING_S3_BUCKET` in `host-setup.sh`'s drill.env template. **The drill renumbered**: 5 replay · 6 recovery order · 7 counts · 8 smoke · 9 bucket listing · **10 erasure rehearsal**.

**Wave 4b — the drill's proof and the mirror:** `dumpGrep` reports **per `COPY` section** — which table holds a token and how many lines there, **never the line itself** — with three new ops cases; drill step 10 as the **seven steps**, both dumps whole, the per-table allow-list read from the script; `backup.sh` pruning the mirror **only when `--porcelain` reports a replaced ref**; `mirror-shell.sh`'s third verb; the deploy-tree cases; ADR 0024's amendment with its README row in the same commit; `BACKUPS.md`'s seven steps and why the two tables hold the set; `RUNBOOK.md`'s two-step release line. The last commit makes **step 7 refuse an empty hash set**, so a host that cannot read the repository fails instead of passing — a check that proves nothing is worse than no check.

### Rulings taken (settled; do not re-open)

1. **The platform bucket: the whole thing** — `S3_BUCKET` and `S3_REGION` on the anchor the api reads, **and** the provisioning in the script that creates the key. *A restore path whose refusal "object store unreachable" stands in production is not a restore path.* No such script existed, so the wizard gained a Garage stage.
2. **The identifier set in the post-erasure dump**: both dumps stay **whole** — a dump taken with tables excluded is not the copy a restore would use, and an exclusion hides the very rows the design keeps. `dump-grep` reports per table; step 10 asserts the tokens absent everywhere **except** `subject_request` and `suppression`, where **present is the expected reading**, because the routine re-creates the suppressions from that set after a restore (spec steps 6 and 10).
3. **ADR 0024 gains its own dated amendment** for `mirror-shell.sh`'s third verb, with its README row, in the same commit as the script.
4. **The rehearsal's seed lives in core** — it writes a concept file and a commit through the concepts slice, and the api tier is transports (ADR 0029). `deploy/seed-synthetic.sh` was **not** grown.
5. **The tester is ralph itself**; no tester child on this ticket.
6. **Root `check` once, at the end**, never beside another. Iteration 6 ran it twice for the reason recorded in *Test feedback*: the first reading's only red was a contention timeout, and the second was taken on a quiet machine.
7. **A rules-page lesson the orchestrator accepted as theirs at landing**: *a core wave that writes new source runs the tag scan in its own iteration's commands.* Three of iteration 2's four tag failures were **wave 1's**, passed a whole iteration earlier by a command list that had no scan in it.

### Hazards that still bind

1. **jCodeMunch's index of this worktree lags a fresh commit.** After 4a it returned `## 6 counts diff` for a line the file reads as `## 7`. **Read the file, not the index**, for anything the last commit touched.
2. **The worktree guard refuses a git command with a `$( )` inside it.** Split it: get the sha, then use it.
3. **One image build at a time**; no root `check` beside another.
4. **A rule tag never appears** in source, a deploy file, a Dockerfile, a CI workflow or a workspace config — this ticket lost an iteration to it.
5. **No migration and no contract change** in T-125.
6. **`runErasure` takes a subject-request id**; a copy carries both ids and the types will not catch a swap.
7. **The rebase moved every sha.** A one-commit ADR proof re-run after the rebase reads the new sha; the mapping is in *Iteration history*.

### For the orchestrator's hygiene lane (outside this ticket)

- **`tests/ops.test.ts` exits 1 on an `ENOTEMPTY` teardown with every test green** (T-142's temp-directory race, now seen in the api suite as well as `erasure-routine.test.ts`). One task: make the git temp-directory teardown tolerate a non-empty directory.
- **`test/rebuild-equivalence.test.ts`'s one case has no headroom under load**: it read 12 s alone and timed out at 60 s with eight sibling vitest processes and three docker builds on the host. It is a T-140 suite and the T-140 rule covers it, but the case's budget is the thinnest of the four.
- **Sweep the operations documents for stale step numbers after S0 lands.** Wave 4b found two stale step-8 references and corrected them; the drill's numbering moved twice in this ticket alone.
- **Both ops documents credited the erasure routine with a mirror prune no code ran** until wave 4b built it. Worth asking what else the documents claim that nothing executes.
- **`packages/core` now exports `./testing/objects`** — the second export from a test path in the tree (`@better-answers/schema/testing` is the first). Worth a convention, or a rule that says there is one.
- **`subject_request` and `suppression` keep the subject's identifiers by design**, so any future "is it gone" check over a whole dump must read per table or be wrong in one direction.
- **`S3_BUCKET` being required makes the next production release two steps** — set the variable, then deploy. In `RUNBOOK.md` now; worth a line wherever release procedure is rehearsed.
- **The drill's runtime is proved only on drill day**: nothing in CI runs `restore-drill.sh`, so the seven steps are held by the deploy-tree test's reading of the script and by the ops suite's cases, never by an execution.
- **`postgresql-client-18` is on VPC 2** (wave 4b's finding) — relevant to step 10's host `pg_dump`.
- Three of five implementor reports in this ticket were **truncated mid-list**, twice in the outside-the-ticket section. Briefs from iteration 4 asked for that list **first**; worth putting in `build-loop.md`.
- `PERSONAL_DATA_CATEGORIES` and `beyondUseFrom` were widened to exported by wave 1.

---

## Test commands

| Acceptance | Command | Last reading |
| --- | --- | --- |
| 1 | `pnpm --filter @better-answers/core exec vitest run test/erasure-replay.test.ts` | 9 passed, 9.28 s |
| 2 | `pnpm --filter @better-answers/core exec vitest run test/erasure-rehearsal.test.ts` | 8 passed, 19.82 s |
| 1,2,3 | `pnpm --filter @better-answers/api exec vitest run tests/ops.test.ts` | 49 passed, 43.32 s; green inside the api `check` |
| 4,5 | `pnpm --filter @better-answers/api exec vitest run tests/deploy-tree.test.ts` | 27 passed, 17.09 s; green inside the api `check` |
| 6 | `pnpm --filter @better-answers/api exec vitest run tests/coding-rules-tags.test.ts tests/adr-index.test.ts` | in the same 27; green inside the api `check` |
| 6 | ADR 0022 · ADR 0024 one-commit checks | both proved (pre-rebase shas) |
| 6 | `pnpm --filter @better-answers/api check` | **green — iteration 6** |
| — | `pnpm --filter @better-answers/core exec vitest run test/erasure-routine.test.ts` | 41 passed, 39.53 s alone |
| — | `pnpm --filter @better-answers/core exec vitest run test/rebuild-equivalence.test.ts` | re-run alone in iteration 6: 2 passed, 15.31 s |
| — | `pnpm check` (root, once, alone) | **green — iteration 6** |

---

## Current iteration

**6 — complete. Verdict PASS at `b70250a`.** Failures on the same issue: **0**. Iterations used: 6 of 10; no issue ever reached two strikes.

**Next focus: none for an implementor.** The loop is closed and the ticket is the orchestrator's:

1. Spawn `verifier` against this worktree with the ticket id.
2. On *APPROVED*, land: the rebase onto `s0` is **already done** and root `check` is **already green at `b70250a`**, so landing is `detect_changes` clean, fast-forward `s0` onto `worktree-agent-ab3c08b87a8f32870` in the main checkout, `git push origin s0`, `node .gitnexus/run.cjs analyze` **in the main checkout**, remove the worktree.
3. Append the Progress entry origin-first, `ordna move T-125 done`, and **open S0's PR** — this ticket is the block's last.

---

## Test feedback

Iteration 6, on the rebased head `b70250a`. Three commands, in order; every one read from its log and never from an exit notification.

**1. The api's `check` — green.** The rebase brought `apps/api/tests/provision-skills.test.ts` into this suite (it arrived on `s0` in `931ac6d`), so the api's test count is three files and one test above the last pre-rebase reading, and it passes.

```
$ pnpm --filter @better-answers/api check
== lint ==     7 anti-slop warnings, 0 errors (tests/harness.ts, tests/flow.ts, src/auth/auth.ts — all pre-existing)
== typecheck == clean
== test ==     Test Files  33 passed (33)
                    Tests  421 passed (421)
                 Duration  47.57s
check passed / EXIT=0
```

**2. Root `check`, first reading — red on one contention timeout, not on an assertion.** Eight sibling vitest processes and three docker builds were on the host (a verifier and an implementor running named suites). The only failure in the whole run:

```
packages/core check:  ❯ test/rebuild-equivalence.test.ts (2 tests | 1 failed) 157645ms
packages/core check:      × finds no mismatch between the two parsers over the bundle those acts wrote 60009ms
packages/core check: Error: Test timed out in 60000ms.
packages/core check:  Test Files  1 failed | 33 passed (34)
packages/core check:       Tests  1 failed | 723 passed (724)
check failed: check:workspaces / EXIT=1
```

Every other workspace was green in that same run: schema 7/437, devtools 6/47, api 33/421, web 8/34 plus 31 browser specs, worker `check passed`. `rebuild-equivalence.test.ts` is one of the four T-140 suites and it touches nothing T-125 wrote — it holds the worker's rebuild against the app's own map.

**3. The T-140 re-run alone — green, and it settles the red.** Contention had fallen to three vitest processes and no docker builds:

```
$ pnpm --filter @better-answers/core exec vitest run test/rebuild-equivalence.test.ts
 Test Files  1 passed (1)
      Tests  2 passed (2)
   Duration  15.31s
```

The case that exhausted a 60 s budget under load runs in about 12 s alone. That is a five-fold stretch from contention, so the red was the host and not the code.

**4. Root `check`, second reading on the quiet machine — green end to end.** Ruling 6 says root `check` runs once; it ran twice here on purpose, because this is the block's last ticket and S0's PR opens on it, and a PASS the orchestrator has to interpret was misread once before (T-124). The isolated re-run above was already enough under the T-140 rule; this makes the reading plain.

```
$ pnpm check        # vitest siblings 3, docker builds 0, no other root check
format:check · lint · jscpd · knip · check:workspaces · check:worker — all green
  packages/schema    7 files / 437 tests
  packages/devtools  6 files /  47 tests
  packages/core     34 files / 724 tests
  apps/api          33 files / 421 tests
  apps/web           8 files /  34 tests + 31 browser specs (12.7s)
  apps/worker      129 passed, 2 warnings, 111.79s
check passed / EXIT=0
```

**Not seen this iteration:** the `ENOTEMPTY` teardown race in `tests/ops.test.ts`. It stays in the hygiene lane as a known flake, but no run in iteration 6 hit it.

---

## Iteration history

- **0** (11/09/2026): plan written from the ticket, the spec's four sections, the ADRs and a read of the tree. Three absences proved with jCodeMunch and citable: no production caller of `openObjects`; no `garage key create` or bucket line in any script; no `platform.erasure.*` act. Approved with the bucket question answered in full and ADR 0024's amendment added.
- **1**: wave 1 — core, the replay's reading half. `0fc87f5`. **PASS**.
- **2**: wave 2 — core, the rehearsal's two phases. `d9205de`. **FAIL** on `tests/coding-rules-tags.test.ts`: four rule tags in source, three of them wave 1's.
- **3**: the four tag citations rewritten as words with the tags cited in `erasure-replay.test.ts`, then wave 3 — the api's two commands. `7be9492`. **PASS**.
- **4**: wave 4a — the recovery order, the one-shot's service, the wizard's Garage stage, ADR 0022. `cfedd55`. **PASS**.
- **5**: wave 4b — `dumpGrep` per table, drill step 10's seven steps, the mirror's prune, `mirror-shell.sh`'s third verb, ADR 0024. `6bdb938` and `d407784`. **PASS** on three readings. The orchestrating session then reached its context limit and paused the loop here, before the two checks.
- **6** (11/09/2026, no implementor): the loop resumed on the paused head. Rebased onto `s0` — clean, no conflict, six commits replayed onto `e37037b`, the branch 6 ahead and 0 behind. The api `check` green (421 tests, now including `provision-skills.test.ts`, which the rebase brought in). Root `check` red once on a `rebuild-equivalence` timeout under eight vitest siblings and three docker builds, that suite green alone at 15.31 s, and root `check` green end to end on the quiet machine. **PASS at `b70250a`.**

**Sha map across the rebase**, read from `git log --oneline s0..HEAD --reverse` in the worktree:

| Wave | Pre-rebase | On `s0` now |
| --- | --- | --- |
| 1 — core, the replay's reading half | `0fc87f5` | `dff01e9` |
| 2 — core, the rehearsal's two phases | `d9205de` | `10d824c` |
| 3 — api, the two commands | `7be9492` | `1aade14` |
| 4a — the recovery order, ADR 0022 | `cfedd55` | `89683d2` |
| 4b — the drill's proof, the mirror, ADR 0024 | `6bdb938` | `b370e73` |
| 4b — step 7 refusing an empty hash set | `d407784` | **`b70250a`** |

**Both ADR one-commit proofs were re-run after the rebase and still hold**, so acceptance line 6 does not rest on a pre-rebase reading:

- `git log -n1 -- docs/adr/0022-*.md` → **`89683d2`**, whose files are `docs/adr/README.md`, both restore scripts, `platform.compose.yaml`, `wizard-41.sh`, `host-setup.sh`, `deploy-tree.test.ts`, `BACKUPS.md`, `RUNBOOK.md` and the ADR.
- `git log -n1 -- docs/adr/0024-*.md` → **`b370e73`**, whose files are `docs/adr/README.md`, `deploy/mirror-shell.sh`, `deploy/backup.sh`, `deploy/restore-drill.sh`, `apps/api/src/ops/index.ts`, both api tests, `BACKUPS.md`, `RUNBOOK.md` and the ADR.

---

## Verification

**Verdict: ✅ APPROVED. Confidence: High.**

Verified at `b70250a`, worktree `/Users/liamj/Documents/development/better-answers/.claude/worktrees/agent-ab3c08b87a8f32870`, tree clean, **6 ahead / 0 behind `s0` (`e37037b`)**. Every acceptance line is ✅; every gate holds. No ❌ and no ⚠️.

Read first: the ordna ticket, this note, `docs/specs/s0-redaction-seam-and-erasure.md` (*The two ops commands*, *The routine*, *The acts on the ledger*, *The mirror*), the ADRs 0020 · 0022 · 0024, and the whole diff `s0..b70250a` (29 files, +3096 / −100).

### Acceptance Criteria

**1 — `replay-erasures`: five answers and the refusals. ✅ VERIFIED.**
Each clause of the line has its own case, and all of them ran green here.

| Clause | Evidence | Verification |
| --- | --- | --- |
| *done* · *replayed 0 erasures*, table present | `tests/ops.test.ts:306` asserts the line word for word | `vitest run tests/ops.test.ts` — 49 passed |
| one request read from the restored rows | `tests/ops.test.ts:310`; core `erasure-replay.test.ts:282` | both suites green |
| one from the replay copy alone when the rows predate it | `tests/ops.test.ts:329` (both rows deleted as superuser first); core `erasure-replay.test.ts:358`, the copy's own pseudonym | both suites green |
| refuses, git root absent | `tests/ops.test.ts:~365` — exit 1, names `GIT_STORE_DIR`, *do not start api* | green |
| refuses, object store unreachable | `tests/ops.test.ts:387` — a door opened on `127.0.0.1:1`, exit 1, *REFUSED* · *do not start api*; also `objects: undefined` at `:377` | green |
| the no-table answer stands | `tests/ops.test.ts:287–295` through `opsBeforeTheJournal` against the cluster's own `postgres` database — *replayed 0 erasures* · *no erasure_request table* | green |

The source agrees with the spec's own sentence: the no-table answer is given **before the doors are opened**, one `io.say` per request in the loop, and each refusal ends *do not start api* (`apps/api/src/ops/index.ts`). The union, the de-duplication by request id and the stop-at-first-failure are core's, with cases at `erasure-replay.test.ts:317`, `:339` and `:477`.

**2 — `erasure-rehearsal`, two phases separable by a dump. ✅ VERIFIED.**
`tests/ops.test.ts:455` asserts the seed's **last line and nothing after it** is the three tokens, which is what the drill reads with `tail -n1`; `:483` asserts `--run` prints the same last line again. The report is not a look-alike: `rehearseErasure` hands back the routine's own report word for word (core `erasure-rehearsal.test.ts:187`), and the api case reads the written file back and finds the real report's fixed sentences, the beyond-use paragraph and *Exports already issued are not recalled.* The phases separate because phase two re-finds the subject from rows by the derived address and takes only the workspace id (`erasure-rehearsal.test.ts:163`); phase two in a workspace phase one never ran in refuses rather than erasing whoever is there (`:176`, and `tests/ops.test.ts`).

**3 — Both ledger events under the platform principal. ✅ VERIFIED.**
`platform.erasure.replayed` (`REPLAY_ACTS`, `replay-erasures.ts:69`) and `platform.erasure.rehearsed` (`rehearsal.ts:82`), which are the two names the spec's *The acts on the ledger* fixes. Rows read back: `tests/ops.test.ts:324` and `:501` both assert `actor: "process:better-answers-erasure"` with the erasure request as subject; core `erasure-rehearsal.test.ts:227` asserts the whole row including the detail, and then asserts **no token appears anywhere in it**.

**4 — The deploy-tree test holds the replay's position and the rehearsal's seven steps. ✅ VERIFIED.**
Two new cases in `apps/api/tests/deploy-tree.test.ts`. The position case loops over **both** scripts and orders four markers — the mirror-bucket object-store sync, `git clone --quiet --bare`, `replay-erasures`, `platform up -d --wait api` — asserting replay > object store, replay > git store, replay < api. It keys on **acts rather than step numbers**, so a renumbering cannot satisfy it quietly. Read against the scripts themselves: `restore-production.sh` is 4 object store · 5 git store · **6 replay** · 7 api; `restore-drill.sh` is 3 object store · 4 git store · **5 replay** · `platform up -d --wait api worker` · 6 recovery order. A third case holds the one-shot on `api` with `--no-deps` and proves `migrate` carries neither `GIT_STORE_DIR` nor `/data/git`.
The seven-step case orders seven act markers — seed · pre-dump · grep found · run · post-dump · grep gone · `cat-file -e` — and additionally holds both dumps whole with no `--exclude`, the per-table allow-list, and step 7's refusal of an empty hash set.
**Numbering note, not a deviation:** the drill's rehearsal is **step 10**, not the ticket's *step 9*. The replay becoming its own numbered step after the two stores — which is this same line's first half — shifts every later step by one. A rehearsal still at step 9 would mean the replay had not moved. The shift is recorded in ADR 0022's amendment, `BACKUPS.md` and `RUNBOOK.md`, and both stale step-8 references were corrected to 9.

**5 — The backup script prunes the mirror after a `--mirror` push that replaced refs, held by the deploy-tree test. ✅ VERIFIED.**
`deploy/backup.sh`'s `job_git_mirror` pushes with `--porcelain` and prunes only when the report carries a `+` (forced update) or `-` (deletion), which is exactly *refs were replaced*; an ordinary fast-forward night prunes nothing. The prune goes over the deploy key's new third verb. `deploy/mirror-shell.sh`'s `prune-repo` is argument-checked as `init-repo` is and runs `reflog expire --expire=now --all` then `gc --prune=now`. The deploy-tree case asserts `push --mirror --porcelain`, the absence of the old `--quiet` form, `grep -qE '^[+-]'` and `prune-repo "${ws}"`; the amended grammar case asserts the third verb, its two refusals, both git lines, **exactly three verb cases** and the catch-all.

**6 — The two ADR amendments with their README rows in the same commit as the code; the documents name the new order; the api's `check` green. ✅ VERIFIED.**
Both one-commit proofs re-run **after the rebase**, so neither rests on a pre-rebase reading:

- `git log -n1 -- docs/adr/0022-*.md` → **`89683d2`**, whose files are the ADR, `docs/adr/README.md`, **both restore scripts**, `platform.compose.yaml`, `wizard-41.sh`, `host-setup.sh`, `deploy-tree.test.ts`, `BACKUPS.md`, `RUNBOOK.md`.
- `git log -n1 -- docs/adr/0024-*.md` → **`b370e73`**, whose files are the ADR, `docs/adr/README.md`, `deploy/mirror-shell.sh`, `deploy/backup.sh`, `deploy/restore-drill.sh`, `apps/api/src/ops/index.ts`, both api tests, `BACKUPS.md`, `RUNBOOK.md`.

`docs/adr/README.md`'s rows 0022 and 0024 both changed, in those two commits and no others. The documents say the new order: `BACKUPS.md` § Recovery order now numbers the three stores at 0 and the replay at 1, adds *Why the replay waits for the stores*, and gains the drill's seven steps as a section headed *step 10*; `RUNBOOK.md` page 1 states the order in a sentence and names the replay as step 6 and not step 3. The api's `check` is green at `b70250a` in the note's *Test feedback* (33 files, 421 tests, 47.57 s, EXIT=0) — a reading taken at this exact head with a clean tree, which stands under the build loop's re-run rule and was not re-run.

### Gates

- **Words before code — held.** `CONTEXT.md` gains *erasure rehearsal*, *synthetic subject* and *token* in `10d824c`, which is also the first commit to carry `rehearsal.ts` — the same commit, not a later one. The earlier commit `dff01e9` names only *replay copy*, already in the glossary.
- **Records with code — held.** Both ADR amendments and both README rows, evidence above.
- **Contract change — not applicable.** No file under `contracts/` and no `contract_version` in the diff.
- **New table, column, grant or definer function — none, and the one new privileged surface is refused both ways.** No migration and no `.sql` in the diff. The mirror deploy key's third verb is the only widened grant: its refusals (*not a workspace id*, *no such mirror*) and the closed three-verb list are asserted beside what it serves.
- **`detect_changes` — held.** `scope: compare`, `base_ref: s0`, on the worktree: 68 changed symbols across the 29 files the ticket touches and nothing outside them. Risk medium. The five affected processes are `graphRebuildCommand` and `waitForJob`, both in `apps/api/src/ops/index.ts`; the diff of that file adds the two erasure commands and does not change either symbol's body.
- **No commit on `main` or the block branch; clean rebase — held.** All six commits are on `worktree-agent-ab3c08b87a8f32870`. `s0` is `e37037b` and the branch is **0 behind**. `main` is `4c77c1d`, untouched. `s0`'s own two commits ahead of `origin/s0` (`931ac6d`, `e37037b`) are the session's, not this ticket's.
- **A measurement in the docblock — not applicable.** The ticket asks for none.

### Risk checks (chosen by what changed)

- **The ops commands' refusals** — three refusal paths, each exit 1 and each ending *do not start api*; usage 2; the no-table answer exit 0 and unchanged; *refused* is never reached by silence, because an unreadable store is an error and never an empty set.
- **The drill's steps as the deploy-tree test holds them** — seven acts in order, both dumps whole, the reading per table with `subject_request` and `suppression` as the expected exception, and step 7 stopping on an empty hash set rather than looping over nothing.
- **The two ADR amendments** — each in one commit with its README row and the code it records.

### Commands run

| Command | Result |
| --- | --- |
| `pnpm --filter @better-answers/core exec vitest run test/erasure-replay.test.ts` | **PASS** — 9 passed, 8.72 s |
| `pnpm --filter @better-answers/core exec vitest run test/erasure-rehearsal.test.ts` | **PASS** — 8 passed, **181.09 s** (19.82 s in the note; contention, green both ways) |
| `pnpm --filter @better-answers/api exec vitest run tests/ops.test.ts` | **PASS** — 49 passed, 100.32 s (43.32 s in the note) |
| `pnpm --filter @better-answers/api exec vitest run tests/deploy-tree.test.ts tests/coding-rules-tags.test.ts tests/adr-index.test.ts` | **PASS** — 27 passed, 7.43 s (the two document scans included) |
| `mcp__gitnexus__detect_changes` (compare against `s0`, on the worktree) | **PASS** — only the ticket's files |
| ADR 0022 · ADR 0024 one-commit proofs, post-rebase | **PASS** — both |
| `pnpm --filter @better-answers/api check` | *not re-run:* recorded green at `b70250a` with a clean tree; HEAD unmoved |
| `pnpm check` (root) | *not re-run:* recorded green at `b70250a` with a clean tree; HEAD unmoved (build loop, *the verifier re-runs root `check` only when HEAD moved past the note's green commit*) |

No `ENOTEMPTY` teardown race in this session's runs of `tests/ops.test.ts`.

### Follow-ups (non-blocking)

1. `test/erasure-rehearsal.test.ts` took **181 s** here against 19.82 s in the note — a nine-fold stretch under three agents' suites. It passed, and no case is near a per-case ceiling, but it is a thicker T-140 candidate than the note's list allows for. Worth a reading alone on a quiet machine before it is trusted as a fast suite.
2. The note's own hygiene list stands unchanged and is worth lifting into ordna `hygiene` tasks at block close: the `ENOTEMPTY` teardown race, `rebuild-equivalence.test.ts`'s thin headroom, the operations documents' step-number sweep, `packages/core`'s second `./testing/*` export wanting a convention, and the drill's runtime being proved only on drill day.
3. `origin/s0` is two commits behind local `s0`. Landing this ticket includes pushing the block branch; worth noticing that those two commits go with it.

### Spec issues (recorded, non-blocking — nothing here was ambiguous enough to block)

1. **The drill's step number in the spec and the ticket is stale by one.** Both say *step 9*; it is step 10, because the same sentence that fixes the number also inserts the replay ahead of it. The build is right and the prose is behind it. Worth a one-line correction to `docs/specs/s0-redaction-seam-and-erasure.md` at block close so the next reader is not misled.
2. **The spec's `dump-grep (absent, expected)` cannot be literally true of the routine the same spec defines.** Steps 6 and 10 keep the identifier set in `subject_request` and `suppression` on purpose, so a post-erasure dump holds it there. The build reads per table and treats *present* in exactly those two as the expected answer, which is the only reading consistent with the routine; the reasoning is in the drill's own comments, `BACKUPS.md` and this note's ruling 2. The spec prose is what wants the amendment, not the code.
